@model IEnumerable<BloodBankManagementSystem.Models.BloodBank>
@{
    ViewData["Title"] = "Blood Banks";
}

<h2 class="page-title">Blood Banks</h2>

<div class="content-box">
    <a href="@Url.Action("Create", "BloodBank")" class="btn add-btn">+ Add New Blood Bank</a>

    <table class="table custom-table">
        <thead>
            <tr>
                <th>Name</th>
                <th>City</th>
                <th>Contact</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var bank in Model)
            {
                <tr>
                    <td>@bank.Name</td>
                    <td>@bank.City</td>
                    <td>@bank.Contact</td>
                    <td>
                        <a href="@Url.Action("Edit", "BloodBank", new { id = bank.BloodBankID })" class="btn action-btn edit-btn">Edit</a>
                        <a href="@Url.Action("Details", "BloodBank", new { id = bank.BloodBankID })" class="btn action-btn view-btn">View</a>
                        <a href="@Url.Action("Delete", "BloodBank", new { id = bank.BloodBankID })" class="btn action-btn delete-btn">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    body{ 
        background: url(/images/dashboard.jpg) no-repeat center center fixed;
         background-size: cover;
     }
    /* Page Title */
    .page-title {
        text-align: center;
        color: white;
        background: linear-gradient(135deg, #8B0000, #600000);
        padding: 14px;
        border-radius: 8px;
        font-size: 1.8rem;
        font-weight: bold;
        letter-spacing: 1px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
    }

    /* Main Content Box */
    .content-box {
        background: white;
        background: url(/images/dashboard.jpg) no-repeat center center fixed;
        background-size: cover;
        border: 1px solid rgba(0, 67, 67, 0.4);
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(5px);
        color: rgb(0, 67, 67);
        font-size: 14px;
        transition: all 0.3s ease-in-out;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.9);
        margin-top: 25px;
        max-width: 95%;
        margin-left: auto;
        margin-right: auto;
        border-left: 5px solid #8B0000;
    }

    /* Add New Blood Bank Button */
    .add-btn {
        display: inline-block;
        background: linear-gradient(135deg, #007a7a, #004d4d);
        color: white;
        font-weight: 600;
        padding: 12px 18px;
        border-radius: 6px;
        text-transform: uppercase;
        border: none;
        transition: all 0.3s ease-in-out;
        cursor: pointer;
    }

        .add-btn:hover {
            background: linear-gradient(135deg, #005f5f, #003b3b);
            transform: scale(1.05);
        }

    /* Custom Table */
    .custom-table {
        margin-top: 20px;
        width: 100%;
        border-collapse: collapse;
        border-radius: 8px;
        overflow: hidden;
    }

        /* Table Header */
        .custom-table thead {
            background: #007a7a;
            color: white;
            text-transform: uppercase;
            font-weight: bold;
        }

        .custom-table th,
        .custom-table td {
            padding: 12px 15px;
            text-align: left;
        }

        .custom-table tbody tr {
            border-bottom: 1px solid #ddd;
            transition: background 0.3s ease-in-out;
        }

            .custom-table tbody tr:hover {
                background: #f1f1f1;
            }

    /* Action Buttons */
    .action-btn {
        font-size: 0.9rem;
        padding: 8px 14px;
        font-weight: 600;
        border-radius: 5px;
        transition: all 0.3s ease-in-out;
        text-decoration: none;
        display: inline-block;
        border: none;
        cursor: pointer;
    }

    /* Edit Button */
    .edit-btn {
        background: linear-gradient(135deg, #f4a261, #e76f51);
        color: white;
    }

        .edit-btn:hover {
            background: linear-gradient(135deg, #e76f51, #d64933);
            transform: scale(1.05);
        }

    /* View Button */
    .view-btn {
        background: linear-gradient(135deg, #1e88e5, #1565c0);
        color: white;
    }

        .view-btn:hover {
            background: linear-gradient(135deg, #1565c0, #0d47a1);
            transform: scale(1.05);
        }

    /* Delete Button */
    .delete-btn {
        background: linear-gradient(135deg, #d32f2f, #b71c1c);
        color: white;
    }

        .delete-btn:hover {
            background: linear-gradient(135deg, #b71c1c, #8b0000);
            transform: scale(1.05);
        }
</style>